# we have given the differnce of subset 1 and subset 2 and you have to find the number of those subsets present in the array.
class solution(object):
    def __init__(self):
        self.dp = list()
    def count_subsets(self, arr, diff):
        pass

    def _recursion(self, arr, diff):
        pass

arr = [1,1,2,3]
diff = 1 
sol = solution()
sol.count_subsets(arr, diff)